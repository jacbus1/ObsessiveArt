# Local personal mode. Never commit your actual .env.
HOST=127.0.0.1
PORT=4173
PUBLIC_URL=http://127.0.0.1:4173
DATA_DIR=./data
NODE_ENV=development
# Optional on loopback; required outside local personal mode.
# Set a UNIQUE 16-256 character secret; do not use an example password.
OBSESSART_PASSWORD=
