# Third-party notices

The shipped application is independently authored JavaScript/HTML/CSS/SVG. It has no third-party runtime packages, remote fonts or bundled font files. It does not embed Obsidian, Miro, Heptabase, tldraw, AFFiNE or BlockSuite code, logos or SDKs. Product names are used solely to describe workflow inspiration.

Optional development infrastructure is not bundled into the application:
- Node.js: its own distribution licenses.
- Playwright 1.57.0: Apache-2.0; browser binaries carry their own licenses.
- GitHub-maintained Actions: their respective repository licenses.
- Docker's Node base image: its bundled software licenses.

System font names in CSS are fallbacks, not redistributed assets. Audit additional dependencies before introducing them. Adding this application's MIT license does not relicense any future third-party code.
