{
  "name": "obsessiveart",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first linked notes, visual whiteboards and knowledge graphs.",
  "license": "MIT",
  "engines": {"node": ">=22"},
  "scripts": {
    "start": "node server.mjs",
    "dev": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/app.js && node --check src/core.js && node --check src/storage.js && node --check sw.js && node --check server.mjs",
    "build": "node scripts-build.mjs"
  }
}
